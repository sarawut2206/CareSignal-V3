# CareSignal V3 — ลูกหลานวัดแรงขาให้ผู้สูงอายุที่บ้าน

> รุ่นที่ปรับหลังฟังผู้ใช้: ไม่ใช้กล้อง ลูกหลานเป็นคนจับเวลาแบบทดสอบมาตรฐาน 3 ท่า ด้วยเกณฑ์เดียวกับรุ่นกล้อง (V2)

🔗 ใช้งาน: https://sarawut2206.github.io/CareSignal-V3/ · รุ่นกล้อง: https://sarawut2206.github.io/CareSignal-V2/

## ทำไมต้องมี V3

ภารกิจ Idea Validation (15–16 ก.ย. 2569) สัมภาษณ์ผู้สูงอายุ 3 คน (อายุ 60, 65, 68) และลูกหลานผู้ดูแล 2 คน:

- 5/5 มีผู้สูงอายุหกล้มในปีที่ผ่านมา · 5/5 อยากประเมินได้ที่บ้าน
- 3/3 ผู้สูงอายุติดตั้งแอปหรือตั้งกล้องเองไม่ได้ ("กดไม่เป็น" · "ตั้งกล้องไม่เป็น")
- 3/3 ผู้สูงอายุเลือก "ให้ลูกหลานรู้" เป็นฟีเจอร์อันดับ 1

จึงเปลี่ยนผู้ใช้จากผู้สูงอายุเป็นลูกหลาน และยกทางสำรอง "จับเวลาด้วยมือ" ของ V2 ขึ้นเป็นทางหลัก

## ไฟล์

| ไฟล์ | คืออะไร |
|---|---|
| `index.html` | หน้ารวมของ V2 ทั้งหมด + ส่วน "เราเปลี่ยนอะไรหลังฟังผู้ใช้" + แผงสมาชิกที่ฝังวิดีโอสาธิต 3 คลิป |
| `CareSignal-App.html` | **ส่วนเดียวที่ต่างจาก V2** — แอปลูกหลาน: ลงทะเบียน · วิดีโอสาธิต · วัดวันนี้ (ลุกนั่ง · ลุกเดิน · ทรงตัว 4 ท่า) · แนวโน้ม · บันทึกว่าล้ม · ใบสรุป · แชร์ (`app.html` เป็นทางลัดมาที่นี่) |
| `CareSignal-Staff.html` `CareSignal-Portfolio-Dashboard.html` `CareSignal-Journey.html` `cs-*.js` `supabase/` | **โคลนจาก V2 ทั้งหมด** — คอนโซลเจ้าหน้าที่และผู้เชี่ยวชาญ แดชบอร์ดบริษัทประกัน ใบส่งต่อ โหมดสาธิต ฐานข้อมูล |
| `testkit.html` | ชุดอุปกรณ์ทดสอบพิมพ์ได้ 3 แผ่น: ของที่ต้องมี · ขั้นตอนติดผนัง · บทวิดีโอสาธิต |
| `cs-score.js` | เกณฑ์คะแนน ยกตัวเลขจาก V2 ทุกตัว (10.0/11.5/12.1 วินาที · MCID 2.3 วินาที · TUG ≥ 12 · ยืนต่อเท้า < 10) |
| `cs-hospitals.js` | รายชื่อโรงพยาบาลจาก V2 |
| `cs-imu.js` `firmware/CareSignal-Waist/` | เซ็นเซอร์คาดเอว (Arduino Nano 33 BLE Sense Rev2): เฟิร์มแวร์ + โมดูลรับค่าทางบลูทูธ วัดแบบห้องตรวจการเคลื่อนไหว (instrumented TUG · 5×STS · การแกว่ง) — เครื่องมือวิจัย/พิสูจน์ความแม่น ใช้ได้บน Android Chrome · `node test/test_imu.mjs` |
| `test/test_score.mjs` | `node test/test_score.mjs` — 28 ข้อ ตรวจเกณฑ์และถ้อยคำที่ห้ามอ้างเกินจริง |

## หลักการเดิมที่ยังอยู่

- ไม่วินิจฉัยโรค ไม่แทนการพบแพทย์ · ฉุกเฉินโทร 1669 ไม่มี "ทีมฉุกเฉิน" ของระบบ
- ข้อมูลบันทึกในเครื่องของลูกหลานก่อน (`localStorage`) แล้วส่งขึ้นระบบกลางเมื่อเชื่อมบัญชี

## ระบบกลางและการส่งต่อ (เหมือน V2)

V3 ใช้ Supabase โปรเจกต์เดียวกับ V2 (`cs-backend.js` ไฟล์เดียวกัน) ผ่านชั้นเชื่อม `cs-cloud.js`

- บัญชี 1 บัญชี = ผู้สูงอายุ 1 คน ลูกหลานเป็นผู้ดูแลบัญชี (เบอร์โทร + PIN 4 หลัก ตามระบบยืนยันตัวตนของ V2) · `profiles.carer_name/carer_phone` เก็บว่าใครวัดให้
- ผลวัด → `assessments` (`method='manual'`, `detail.measured_by='carer'`) → `risk_signals` → ทริกเกอร์ `open_case_on_signal` เปิดเคสในคอนโซลเจ้าหน้าที่ V2 → ระดับส้ม/แดงสร้าง `referrals`
- "บันทึกว่าล้ม" → `care_events(kind='fall')` → ทริกเกอร์ใหม่ `open_case_on_fall_event` (migration 23) เปิดหรือยกระดับเคสทันที
- ต้องรัน `supabase/23_v3_carer.sql` บนโปรเจกต์หนึ่งครั้ง (รันซ้ำได้) — ถ้ายังไม่รัน แอปยังส่งผลได้ แต่การล้มจะไม่เปิดเคสอัตโนมัติ
- ทุกสัญญาณเสี่ยงแสดงเหตุผลและที่มา ไม่ใช่ AI ทำนาย
- ค่าจับเวลาที่ผิดปกติ (เช่น 0 วินาที) จะถูกถามยืนยันก่อนบันทึก

## วิดีโอสาธิตและเครดิต

วิดีโอ 3 คลิปเป็นผลงานชุด "สูงวัยไม่ล้ม BWSTT" ของช่อง YouTube [Siriraj Health Policy](https://www.youtube.com/@SirirajHealthPolicy) ฝังผ่านเครื่องเล่น YouTube พร้อมเครดิตในทุกจุดที่แสดง ลิขสิทธิ์เป็นของเจ้าของผลงาน

- [การลุกขึ้นยืน 5 ครั้ง (Five Times Sit-to-Stand Test)](https://www.youtube.com/watch?v=KQaDdX66OUM)
- [Timed Up and Go Test](https://www.youtube.com/watch?v=o_HM_3u0TZk)
- [การยืนเพื่อทดสอบการทรงตัว 4 แบบ (4-Stage Balance Test)](https://www.youtube.com/watch?v=AO88b5YLFg0)

เปลี่ยนคลิปได้ที่ตัวแปร `VIDEOS` ใน `CareSignal-App.html` และส่วน `#g-member` ใน `index.html`
